# Default config for Docker mode
# For local development, override with:
#   KAFKA_BOOTSTRAP_SERVERS=localhost:9094 cargo run

[kafka]
brokers = "kafka:9092"
group_id = "ics-parser"
input_topic = "ics.raw.packets"
client_id = "ics-parser"
auto_offset_reset = "earliest"

[metrics]
port = 8082
