{
  "name": "@ics/parser",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "cargo test",
    "test:verbose": "cargo test -- --nocapture",
    "lint": "cargo clippy -- -D warnings",
    "build": "cargo build --release"
  }
}
