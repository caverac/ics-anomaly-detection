{
  "name": "@ics/capture",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "go test -v ./...",
    "test:coverage": "go test -coverprofile=coverage.out ./... && go tool cover -func=coverage.out | grep total",
    "lint": "golangci-lint run",
    "build": "go build -o bin/capture ./cmd/capture"
  }
}
