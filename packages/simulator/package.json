{
  "name": "@ics/simulator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "python -m pytest src/ -v || echo 'No tests yet'",
    "lint": "ruff check src/",
    "lint:fix": "ruff check --fix src/",
    "format": "ruff format src/",
    "start": "python src/main.py"
  }
}
