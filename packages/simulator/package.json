{
  "name": "@ics/simulator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "mise exec -- python -m pytest src/ -v || echo 'No tests yet'",
    "lint": "mise exec -- python -m ruff check src/",
    "start": "mise exec -- python src/main.py"
  }
}
